<!doctype html>
<html lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="theme-color" content="#63cf05">

  <title>
    {{ page_title -}}
    {%- if current_tags %} &ndash; {{ current_tags | join: ', ' }}{%- endif -%}
    {%- if current_page != 1 %} &ndash; Pagina {{ current_page }}{%- endif -%}
    {%- unless page_title contains shop.name %} &ndash; {{ shop.name }}{%- endunless -%}
  </title>

  {%- if page_description -%}
    <meta name="description" content="{{ page_description | escape }}">
  {%- endif -%}

  {{ content_for_header }}

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@600;700;800&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  {{ 'theme.css' | asset_url | stylesheet_tag }}
</head>
<body class="template-{{ template.name }}">

  {% section 'header' %}

  <main id="main-content" tabindex="-1">
    {{ content_for_layout }}
  </main>

  {% section 'footer' %}

  {% render 'cart-drawer' %}

  <!-- Mobile navigation -->
  <div class="mobile-nav" id="mobile-nav" aria-hidden="true" role="dialog" aria-label="Menu">
    <div class="mobile-nav-header">
      <a href="{{ routes.root_url }}" class="header-logo-wrap" aria-label="{{ shop.name }}">
        {%- if settings.logo != blank -%}
          {{ settings.logo | image_url: width: 200 | image_tag: class: 'header-logo-img', alt: shop.name }}
        {%- else -%}
          <span class="header-logo-icon" aria-hidden="true">
            <svg width="22" height="22" viewBox="0 0 48 48" aria-hidden="true">
              <circle cx="24" cy="24" r="23" fill="#fff"/>
              <path d="M9 24c4-5 9-7 14-7 4 0 8 2 12 5l3-2-1 5 1 5-3-2c-4 3-8 5-12 5-5 0-10-2-14-7z" fill="#63cf05"/>
              <circle cx="34" cy="22" r="1.3" fill="#0e1814"/>
            </svg>
          </span>
          <span class="header-logo-wordmark">
            <span class="header-logo-name">{{ shop.name }}</span>
          </span>
        {%- endif -%}
      </a>
      <button class="cart-close-btn" id="mobile-nav-close" aria-label="Menu sluiten">&#x2715;</button>
    </div>
    <nav class="mobile-nav-links">
      <a href="{{ routes.root_url }}">Home</a>
      <a href="{{ routes.collections_url }}">Shop</a>
      <a href="/collections/karpervissen">Karpervissen</a>
      <a href="/collections/meervalvissen">Meervalvissen</a>
      <a href="/collections/steurvissen">Steurvissen</a>
      <a href="/collections/methodfeeder">Method Feeder</a>
      <a href="/collections/waggler">Waggler</a>
      <a href="/collections/koi-vijver">Koi &amp; Vijver</a>
      <a href="/collections/tools-merchandise">Tools &amp; Merch</a>
      <a href="/pages/clinics" style="color:var(--mid-green)">Clinics</a>
      <a href="/pages/zakelijk" style="color:var(--mid-green)">Zakelijk</a>
      {%- if pages['over-ons'] -%}<a href="{{ pages['over-ons'].url }}">Over ons</a>{%- endif -%}
      {%- if pages['contact'] -%}<a href="{{ pages['contact'].url }}">Contact</a>{%- endif -%}
    </nav>
  </div>

  <div class="cart-overlay" id="cart-overlay" aria-hidden="true"></div>

  {%- if settings.whatsapp_number != blank -%}
    <a href="https://wa.me/{{ settings.whatsapp_number | remove: '+' | remove: ' ' | remove: '-' }}"
       target="_blank"
       rel="noopener"
       class="wa-btn"
       aria-label="Neem contact op via WhatsApp">
      <svg width="30" height="30" viewBox="0 0 32 32" fill="currentColor" aria-hidden="true">
        <path d="M16 3C8.8 3 3 8.8 3 16c0 2.6.8 5 2.1 7L3 29l6.2-2c1.9 1 4.1 1.6 6.3 1.6 7.2 0 13-5.8 13-13S23.2 3 16 3zm0 23.6c-2 0-3.9-.6-5.5-1.5l-.4-.2-3.7 1.2 1.2-3.6-.3-.4A10.6 10.6 0 1 1 16 26.6zm6-7.9c-.3-.2-2-1-2.3-1.1-.3-.1-.5-.2-.8.2s-.9 1.1-1.1 1.3-.4.2-.7 0c-.3-.2-1.4-.5-2.7-1.6-1-.9-1.7-2-1.9-2.4-.2-.3 0-.5.2-.7l.5-.6c.2-.2.2-.3.4-.5.1-.2.1-.4 0-.5-.1-.2-.7-1.8-1-2.5-.3-.6-.6-.5-.8-.5h-.6c-.2 0-.5.1-.8.4-.3.3-1 1-1 2.5s1 2.9 1.2 3.1c.2.2 2 3.1 5 4.4.7.3 1.2.5 1.7.6.7.2 1.3.2 1.8.1.6-.1 1.7-.7 2-1.4.3-.7.3-1.2.2-1.4-.1-.2-.3-.3-.6-.4z"/>
      </svg>
    </a>
  {%- endif -%}

  <script src="{{ 'theme.js' | asset_url }}" defer></script>
</body>
</html>
