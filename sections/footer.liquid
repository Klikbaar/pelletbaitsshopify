<footer class="site-footer" role="contentinfo">
  <div class="container">
    <div class="footer-grid">

      <div>
        <div class="footer-logo-wrap">
          {%- if section.settings.logo != blank -%}
            {{ section.settings.logo | image_url: width: 200 | image_tag: class: 'footer-logo-img', alt: shop.name }}
          {%- else -%}
            <span class="footer-logo-text">{{ shop.name | upcase }}</span>
          {%- endif -%}
        </div>
        <p class="footer-desc">{{ section.settings.description }}</p>
        <div class="footer-contact" style="margin-top:20px;display:flex;flex-direction:column;gap:8px;font-size:13px;color:var(--ink-3);">
          {%- if settings.email != blank -%}
            <a href="mailto:{{ settings.email }}" style="transition:color .14s ease;">{{ settings.email }}</a>
          {%- endif -%}
          {%- if settings.phone != blank -%}
            <a href="tel:{{ settings.phone | remove: ' ' | remove: '-' }}" style="transition:color .14s ease;">{{ settings.phone }}</a>
          {%- endif -%}
          {%- if settings.address != blank -%}
            <span>{{ settings.address }}</span>
          {%- endif -%}
        </div>
      </div>

      <div>
        <p class="footer-col-title eyebrow">{{ 'footer.nav_title' | t }}</p>
        <ul class="footer-links">
          <li><a href="{{ routes.root_url }}">{{ 'nav.home' | t }}</a></li>
          <li><a href="/collections">{{ 'nav.shop' | t }}</a></li>
          {%- if pages['over-ons'] -%}
            <li><a href="{{ pages['over-ons'].url }}">{{ 'nav.about' | t }}</a></li>
          {%- else -%}
            <li><a href="/pages/over-ons">{{ 'nav.about' | t }}</a></li>
          {%- endif -%}
          {%- if pages['klantenservice'] -%}
            <li><a href="{{ pages['klantenservice'].url }}">{{ 'nav.customer_service' | t }}</a></li>
          {%- else -%}
            <li><a href="/pages/klantenservice">{{ 'nav.customer_service' | t }}</a></li>
          {%- endif -%}
          {%- if pages['contact'] -%}
            <li><a href="{{ pages['contact'].url }}">{{ 'nav.contact' | t }}</a></li>
          {%- else -%}
            <li><a href="/pages/contact">{{ 'nav.contact' | t }}</a></li>
          {%- endif -%}
        </ul>
      </div>

      <div>
        <p class="footer-col-title eyebrow">{{ 'footer.newsletter_title' | t }}</p>
        <p class="footer-newsletter-body">{{ section.settings.newsletter_text }}</p>
        <form method="post" action="/contact" accept-charset="UTF-8">
          <input type="hidden" name="form_type" value="customer">
          <input type="hidden" name="utf8" value="&#x2713;">
          <input type="hidden" name="contact[tags]" value="newsletter">
          <div class="newsletter-form">
            <label class="visually-hidden" for="newsletter-email">E-mailadres</label>
            <input class="newsletter-input" id="newsletter-email" type="email" name="contact[email]" placeholder="jouw@email.nl" autocomplete="email" required>
            <button class="newsletter-submit" type="submit">{{ 'footer.newsletter_submit' | t }}</button>
          </div>
        </form>
        <p class="newsletter-note">{{ 'footer.newsletter_note' | t }} <a href="/pages/privacybeleid">privacybeleid</a>.</p>
      </div>

    </div>

    <div class="footer-bottom">
      <p>&copy; {{ 'now' | date: '%Y' }} {{ shop.name }}. {{ 'footer.rights' | t }}</p>
      <div class="footer-bottom-links">
        {%- for link in linklists[section.settings.legal_menu].links -%}
          <a href="{{ link.url }}">{{ link.title }}</a>
        {%- endfor -%}
        <a href="/pages/algemenevoorwaarden">Algemene voorwaarden</a>
        <a href="/pages/privacybeleid">Privacybeleid</a>
      </div>
    </div>
  </div>
</footer>

{% schema %}
{
  "name": "Footer",
  "settings": [
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Omschrijving",
      "default": "De specialist in vispellets. Officieel distributeur van Alltech Coppens."
    },
    {
      "type": "link_list",
      "id": "legal_menu",
      "label": "Juridisch menu"
    },
    {
      "type": "text",
      "id": "newsletter_text",
      "label": "Nieuwsbrief tekst",
      "default": "Blijf op de hoogte van aanbiedingen bij Pellet Baits!"
    }
  ]
}
{% endschema %}
